{
  "name": "table",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "wasm-pack build --target web --no-typescript --out-name wasm --out-dir ./static/build",
    "start": "npx serve ./static"
  },
  "keywords": [],
  "author": ""
}
